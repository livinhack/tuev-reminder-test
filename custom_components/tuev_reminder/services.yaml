confirm_passed:
  name: Confirm passed inspection
  description: Updates the next inspection due date based on the current month and the configured interval.
  fields:
    entity_id:
      name: Entity
      description: TÜV Reminder sensor to update.
      required: true
      selector:
        entity:
          domain: sensor