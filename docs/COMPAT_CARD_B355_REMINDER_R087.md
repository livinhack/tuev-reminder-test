# Compatibility – Card b355 + Reminder r087

Reminder r087 does not change the vehicle schema, Manager API payloads or Card bridge attributes.

Compatibility remains the same as r086:

- Card b355 or newer remains the expected baseline.
- Existing Reminder entities and ConfigEntries do not need migration.
- r087 only changes Sidebar empty/search-state behavior and local UI affordances.
