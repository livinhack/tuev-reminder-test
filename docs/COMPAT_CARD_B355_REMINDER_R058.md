# Compatibility – Card b355 with Reminder r058

r058 changes only Sidebar list rendering state preservation. Manager CRUD payloads, sensor attributes, calendar behavior, services, Card bridge fields, panel registration, responsive table rules, and brand asset paths are unchanged from r057.

The Lovelace/Dashboard Card remains a separate project. Reminder continues to own the data/ConfigEntries; the Card only reads the resulting entities and attributes.
