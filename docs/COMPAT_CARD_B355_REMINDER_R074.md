# Compatibility – Card b355 / Reminder r074

Reminder r074 does not change sensor attributes, Card bridge fields, service names or Card-facing data semantics.

Card remains a separate Dashboard/Lovelace project. Reminder r074 only adds release/HACS metadata guard checks and keeps the Sidebar CRUD stack intact.

Expected Card impact: none compared with r073.
