# Compatibility – Card b355 + Reminder r089

Reminder r089 does not change the Card bridge/payload contract from the recent Sidebar-only builds. It only adjusts the Sidebar vehicle-list markup/CSS around the neutral plate text slot.

Expected compatibility with Card b355 remains unchanged.
