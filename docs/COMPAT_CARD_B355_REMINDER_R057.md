# Compatibility – Card b355 with Reminder r057

r057 changes only the Reminder Manager backend validation path for Sidebar create/update commands. Sensor attributes, Card bridge fields, calendar behavior, services, panel registration, responsive Sidebar UI and brand asset paths are unchanged from r056.

The Lovelace/Dashboard Card remains a separate project and is not bundled or imported by the Reminder integration.
