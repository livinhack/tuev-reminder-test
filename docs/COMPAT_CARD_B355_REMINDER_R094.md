# Compatibility – Card b355 with Reminder r094

Reminder r094 remains compatible with Card b355 because it only changes Sidebar frontend sort-summary visibility and the internal Reminder working version. No Card files, Card bridge attributes, Manager API schema or vehicle payload fields were changed.
