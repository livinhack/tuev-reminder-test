# Compatibility – Card b355+ / b356 RC with Reminder r035

Reminder r035 keeps the Card bridge attributes and read-only manager records from r030-r033 intact.

The new Sidebar form skeleton is Reminder-owned UI only. It does not import Card files, does not call Card actions and does not change the Dashboard Card contract.
