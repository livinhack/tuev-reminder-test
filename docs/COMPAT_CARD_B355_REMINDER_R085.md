# Compatibility – Card b355 + Reminder r085

Reminder r085 is a Sidebar frontend layout polish step only. It keeps the existing Reminder Manager API shape and Card bridge behavior from the previous r-series builds.

## Compatibility

- Compatible baseline remains Card b355.
- No Card files are included or changed.
- No API field migration is introduced.
- Plate preview changes are Sidebar-only and temporary until Card renderer detection is implemented.
