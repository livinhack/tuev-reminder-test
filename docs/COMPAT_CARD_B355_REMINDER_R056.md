# Compatibility – Card b355 with Reminder r056

r056 changes only the Reminder Sidebar table action identity handling. The Manager API payloads, sensor attributes, Card bridge fields, calendar behavior, create/update/delete commands and brand asset paths are unchanged from r055.

The Lovelace/Dashboard Card remains a separate project and continues to read Reminder entities/attributes as before.
