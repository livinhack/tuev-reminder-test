# Compatibility – Card b355+ with Reminder r053

Reminder r053 keeps the existing entity attributes and Card bridge fields unchanged. It only changes the Sidebar manager touch target for the row action menu.
