# Compatibility – Card b355 / Reminder r088

Reminder r088 does not change the Manager API, entity attributes, config schema or Card bridge payloads. Compatibility with Card b355 remains unchanged from the previous r08x Sidebar UI-only builds.

The only relevant UI difference is inside the Reminder Sidebar list: the temporary pseudo-plate graphic is removed from list rows and replaced by neutral plain plate text.
